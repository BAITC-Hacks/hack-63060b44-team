# Проверки перед сдачей — 23 сентября 2026

Этот протокол описывает действия, действительно выполненные при подготовке к сдаче. Он отделён от ранее сохранённых результатов обучения. Сеть не использовалась, исходные CSV и погодный кэш не менялись, большой архив не создавался. Коммит и push не выполнялись.

## Исходное состояние

- HEAD: `53c18a8` (`Initial commit`). README уже имел изменения; код, конфигурация, тесты, документация и отчёты уже существовали как untracked-файлы. Ни сброса, ни удаления этих файлов не было.
- [initial_state.json](initial_state.json) содержит исходный статус, diff README и SHA-256 50 существовавших файлов вне игнорируемых каталогов.
- Прочитаны README, конфигурация, исходники, тесты, сохранённые отчёты и обе страницы PDF. Локальных `AGENTS.md` в проекте и проверенных родительских каталогах не найдено.
- SHA-256 конкурсного PDF: `6ac2bfb02bd8abf3a85235df492386f5bc9608188a0c8dbb084cfbb95b2deaa3`. Требования цикла и баллы 25/25/25/15/10 соответствуют документации; PDF не требует LLM и не указывает точный час ежедневного выпуска.

## Выполненные команды и результаты

Среда: Windows, Python 3.12.14, pytest 9.1.1. В песочнице этой машины загрузка нативной библиотеки NumPy блокируется, поэтому вычислительные проверки выполнены с разрешённым запуском вне песочницы. Это ограничение среды проверки, не отсутствие библиотеки проекта.

| Проверка | Что выполнено сейчас | Результат |
|---|---|---|
| Полный набор после исправления отчётных скриптов | `.venv\Scripts\python -m pytest -q` | **46 passed, 9 subtests passed in 6.65s**, exit 0 |
| Повторяемость первого и последнего выпусков | `python -m scripts.check_reproduction --output reports/submission/reproduction_check.json` | Обе модели повторно обучены из снимков, максимальная разница **0.0**; повтор первого запуска вернул тот же каталог, манифест не изменён; exit 0 |
| CLI-прогноз из кэша | `python -m windforecast forecast --issue "2026-01-31T23:00:00+05:00" --weather cache` | Вернул существующий `a587f5347a6fc4512eec87fc`, событие `unchanged_inputs`; exit 0; это повторная проверка входов и повторное использование результата |
| Демонстрация обновления и загрузки | Два существующих теста, команды ниже | **2 passed in 2.82s**, exit 0; это подмножество 46 тестов, не ещё два новых теста |
| CLI-аудит без перезаписи прежнего отчёта | `python -m windforecast --config tmp/submission_audit/config.json audit` | Отдельный временный конфиг с теми же абсолютными путями исходников; полученный JSON структурно равен `reports/data_audit.json`, исходный отчёт сохранён |
| Проверка конкретного каталога через CLI | `python -m windforecast verify-run artifacts/runs/a587f5347a6fc4512eec87fc` | `status=reproduced`, разница **0.0**, exit 0 |
| Синтаксис исторического replay | `python -m windforecast replay --help` и чтение CLI | Параметры `--start`, `--end`, `--horizon`, `--model`, `--weather`, `--continue-on-error` подтверждены; весь replay заново не запускался |
| Сверка первичных данных и чисел отчётов | Независимое чтение CSV, прогнозов и метрик | **88 проверок прошли**, без обучения; подробности в [evidence_check.json](evidence_check.json) |
| Инвентаризация передачи | Чтение размеров и проверка SHA-256 существующих файлов | Подробности в [handoff_inventory.json](handoff_inventory.json); сеть и копирование больших каталогов не выполнялись |

Команды двух изолированных демонстрационных тестов:

```powershell
.venv\Scripts\python -m pytest -v tests/test_pipeline.py::PipelineTests::test_changed_input_recomputes_and_unchanged_input_is_idempotent tests/test_data.py::ObservationTests::test_ingest_immutable_idempotent_and_causal
```

Это искусственные январские фикстуры во временных каталогах: persistence, без погоды, проверка нового результата при изменении входа и причинной доступности загружаемой партии. Они не подменяют реальные погодные данные и не являются оценкой февраля. Рабочие CSV и конкурсные результаты не редактируются.

## Что подтвердили первичные файлы

- 142 360 / 149 499 строк за 11.03.2023–31.01.2026; ни одного февральского измерения. Локальные копии побайтно совпадают с переданными оригиналами на Desktop.
- 141 357 общих меток; 1 003 / 8 142 метки присутствуют только у одной турбины. Соединение по времени соответствует фактам.
- 29 выпусков по 48 часов = **1 392 строки**, **720 уникальных целевых часов**; 1 320 строк относятся к февралю, 72 — к марту.
- Независимо пересчитаны 1 989 строк метрик по сохранённым предсказаниям, а фактические метки сверены с исходными CSV. Максимальное численное расхождение — около `2.22e-16` при допуске `1e-12`. Метрики вручную не исправлялись.
- Проверены 76 манифестов валидации и 29 манифестов replay, хеши, текущая версия исходников, временная сетка и допустимость погодных выпусков. Возраст измерений на последнем выпуске — **671 час** для обеих турбин.
- При выборе по 22–26 января все метки известны к `2026-01-28 19:00Z`, раньше контрольного выпуска `2026-01-29 18:00Z`; общих целевых часов нет. Это ретроспективное хронологическое разделение сохранённых прогнозов. Полные семь выпусков имеют 24 общих целевых часа с контролем и не образуют с ним независимый тест.

Февральская фактическая точность, обобщение на будущие сезоны и качество при месяце без новых измерений не подтверждены. `UTC+05:00`, начало интервала, равновесный индекс и час выпуска остаются проектными допущениями. Погодные ограничения и вопросы организаторам указаны в README.

## Внесённые изменения

- **README.md:** вопросы организаторам, сценарий 3–5 минут, точное число уникальных часов, устаревание входов, характер контрольной проверки, роль детерминированной оркестрации и ссылки для передачи.
- **docs/handoff.md, handoff_inventory.json:** реальные размеры и состав комплектов, необходимые/необязательные файлы, команды восстановления и переносимость.
- **scripts/check_reproduction.py:** убрано неподтверждённое автоматическое заявление о количестве тестов; добавлен `--output`, чтобы сохранить новый протокол отдельно. Скрипт честно пишет `test_suite.status=not_run_by_this_script`.
- **scripts/summarize_results.py, reports/results.md:** убрана безусловная строка о прохождении тестов, добавлена ссылка на фактический протокол. Численные таблицы не менялись. Прежний `reports/reproducibility.json` сохранён как историческое свидетельство.
- **.gitattributes:** `windforecast/*.py -text` сохраняет точные байты исходников при Git checkout. Это важно, потому что `verify-run` проверяет SHA-256 байтов, а на этой машине включён `core.autocrlf=true`.
- **.gitignore:** явно исключён `.uv-cache/`. Прежние исключения `.venv/`, `data/`, `artifacts/`, `.env`, `config.local.json`, временных файлов сохранены.
- **reports/submission/**: отдельные протоколы исходного состояния, проверки чисел, повторяемости и передачи. Алгоритмы в `windforecast/`, конфигурация, исходные измерения и численные отчёты валидации сохранены.

Новый сборщик итогового отчёта и изменённый скрипт проверки синтаксически проверены; сам отчёт не выдаёт свои статические строки за новый запуск pytest. Повторно весь процесс обучения/валидации и полный февральский replay не выполнялись: проверялись сохранённые результаты и заново обучались только выбранные снимки.

## Git и комплект сдачи

Коммит, push и staging не выполнялись; HEAD остаётся исходным. В будущую сдачу включить README, `.gitignore`, `.gitattributes`, конфигурацию без секретов, `pyproject.toml`, `requirements.lock`, `windforecast/`, `tests/`, `scripts/`, `docs/` и небольшие `reports/`.

Данные и снимки передавать отдельно по [манифесту](../../docs/handoff.md). Не включать `.venv`, `.uv-cache`, `.pytest_cache`, `__pycache__`, `.egg-info`, `.env`, `config.local.json`, `tmp/` и лишние экспериментальные запуски. Финальная сверка с исходным состоянием: [final_state.json](final_state.json).
